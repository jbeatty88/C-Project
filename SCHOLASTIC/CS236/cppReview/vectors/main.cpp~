#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char**) {
  vector<int> v1;

  for(int i = 1; i <= 5; i++)
    v1.push_back(i);

  cout << "SINGLE OUTPUT FOR EACH: " << endl;
  cout << v1.begin() << endl;
  cout << v1.end() << endl;
  return 0;
}
